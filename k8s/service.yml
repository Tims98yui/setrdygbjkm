apiVersion: v1
kind: Service
metadata:
  name: sryfhdcgb
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "sryfhdcgb"
    port: 8080
  selector:
    app: sryfhdcgb
